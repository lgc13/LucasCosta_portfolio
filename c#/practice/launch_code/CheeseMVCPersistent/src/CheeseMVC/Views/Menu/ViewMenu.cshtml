@{
    ViewBag.Title = "Menu";
}

<h2>@Model.Menu.Name</h2>

<table class="table">

    <ul>
        @foreach (var cheeseMenu in Model.Items)
        {
            <li>
                @cheeseMenu.Cheese.Name
            </li>
        }
    </ul>

</table>

@if (Model.Items.Count == 0)
{
    <p>No cheeses yet!</p>
}

<p><a asp-controller="Menu" asp-action="AddItem" asp-route-id="@Model.Menu.ID">Add Item</a></p>
